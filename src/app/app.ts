import {Component} from '@angular/core'

@Component({
  selector   : 'app',
  templateUrl: './app.html',
  styles: [`
    main {
      margin-top: 50px;
    }
  `]
})
export class AppComponent {
}
